#include <iostream>
#include <vector>
#include <algorithm>
#include <bits/stdc++.h>
#include <string>
#include <stdio>

using namespace std;

class Segment {
public:
    int min;
    int max;
    int pos;
};
int main() {
    ios_base::sync_with_stdio(false);    
    cin.tie(NULL);

    std::list<Segment> h_segs;
    std::list<Segment> v_segs;
    
    string wire1;
    cin >> wire1; 
    cout << wire1;
    string token;
    do {
	token = strtok(NULL, ',');
	list.push_back(	
    } while (token != NULL);
}
